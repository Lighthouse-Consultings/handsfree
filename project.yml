name: Handsfree
options:
  bundleIdPrefix: com.lighthouseconsultings
  deploymentTarget:
    macOS: "14.0"
  createIntermediateGroups: true
  xcodeVersion: "16.0"

settings:
  base:
    SWIFT_VERSION: "5.9"
    MACOSX_DEPLOYMENT_TARGET: "14.0"
    DEVELOPMENT_TEAM: ""
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: YES

targets:
  Handsfree:
    type: application
    platform: macOS
    sources:
      - path: Handsfree
    resources:
      - path: Handsfree/Resources
    info:
      path: Handsfree/Info.plist
      properties:
        LSUIElement: true
        NSMicrophoneUsageDescription: "Handsfree uses the microphone to transcribe your speech into text."
        CFBundleName: Handsfree
        CFBundleShortVersionString: "0.1.0"
        CFBundleVersion: "1"
    entitlements:
      path: Handsfree/Handsfree.entitlements
      properties:
        com.apple.security.app-sandbox: false
        com.apple.security.device.audio-input: true
        com.apple.security.network.client: true
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.lighthouseconsultings.handsfree
        GENERATE_INFOPLIST_FILE: NO
        INFOPLIST_FILE: Handsfree/Info.plist

  HandsfreeTests:
    type: bundle.unit-test
    platform: macOS
    sources:
      - path: Tests
    dependencies:
      - target: Handsfree
